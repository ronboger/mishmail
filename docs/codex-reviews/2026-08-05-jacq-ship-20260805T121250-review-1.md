---
date: 2026-08-05 12:15
kind: fable-iterate review
grok_jobs: ship-20260805T121250-92479
implementer_model: grok-4.5
reviewed_commit: 95d0bbf9e4e4afc7b1929686b1b96ffbb93cdf26
branch: jacq/ship-20260805T121250-92479
verdict: SHIP
---

# Review — bottom-leading undo toast + ⌘Z cancel-send

Scope: move the interactive undo toast to bottom-leading (keep clickable);
make ⌘Z reliably cancel a pending undo-send when orphan text focus would
otherwise swallow it.

## Pass 1 (95d0bbf)

**Task 1 — toast reposition.** The two overlays were merged into one
`overlay(alignment: .bottomLeading)` holding a `VStack(alignment: .leading,
spacing: 10)` with the notice capsule above the undo capsule. Each capsule
keeps its original font/padding/material/shadow/transition; the notice keeps
`allowsHitTesting(false)` (undo does not, so the Undo button stays
clickable). Stack padding `.leading, 20` / `.bottom, 20` matches the prior
notice placement. Presence-driven animations
(`UndoToast.isPresented(store.undoAction)` and `store.notice`) unchanged.
Stale comments in ContentView and ThreadListView updated. Overlap between
simultaneous notice + undo is now a stack instead of a collision — correct.

**Task 2 — ⌘Z cancel-send.** New pure rule
`ComposeKeyOwnership.undoChordBypassesTextFocus(pendingSend:composeClaimsTyping:)`
= `pendingSend && !composeClaimsTyping`, applied inside the ⌘Z guard so the
text-focus block is defeated only while an undo-send window is open and no
expanded compose claims typing. Boolean restructure verified:
`!(editing && blocks)` became `!(editing && blocks && !bypass)` — bypass
true ⇒ chord fires; all prior behavior identical when bypass is false.
Expanded-compose text undo untouched. The existing `finishing` bypass is
preserved.

**Tests.** Three new unit tests on the pure rule (bypass, compose-typing
override, no-pending default) in ComposeKeyOwnershipTests, matching file
style. Full gate: `make test` → 1322 tests, 0 failures (run by supervisor;
Grok's sandbox blocked SPM resolution — `--no-verify` commit was
environment-only, not a skipped-failure).

**Unrequested changes:** none. Diff confined to the four expected files.

Verdict: **SHIP**.
